export default function GamesLayout({ children }: { children: React.ReactNode }) {

    return (
        <div className="">
            <h1>Games Layout</h1>
            <p>{children}</p>
        </div>
    )
}